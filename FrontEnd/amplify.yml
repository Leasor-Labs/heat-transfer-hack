version: 1
frontend:
  phases:
    build:
      commands: []
  artifacts:
    # IMPORTANT: Change this to your actual folder structure
    baseDirectory: frontend
    files:
      - '**/*'
  cache:
    paths: []
