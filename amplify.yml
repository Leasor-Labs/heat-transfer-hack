version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        # Repo root (Amplify may run from parent of heat-transfer-hack)
        - |
          REPO_ROOT="${CODEBUILD_SRC_DIR:-.}"
          [ -d "heat-transfer-hack" ] && REPO_ROOT="heat-transfer-hack"
          cd "$REPO_ROOT"
          chmod +x node_modules/.bin/* 2>/dev/null || true
        # Seed DynamoDB with Toledo data (only if tables exist; set env in Amplify if you use SAM backend)
        - |
          REPO_ROOT="${CODEBUILD_SRC_DIR:-.}"
          [ -d "heat-transfer-hack" ] && REPO_ROOT="heat-transfer-hack"
          cd "$REPO_ROOT"
          export HEAT_SOURCES_TABLE="${HEAT_SOURCES_TABLE:-HeatSources}"
          export HEAT_CONSUMERS_TABLE="${HEAT_CONSUMERS_TABLE:-HeatConsumers}"
          export AWS_REGION="${AWS_REGION:-us-east-1}"
          if [ -n "$HEAT_SOURCES_TABLE" ] && [ -n "$HEAT_CONSUMERS_TABLE" ]; then
            npx ts-node src/data/seedDynamoFromToledo.ts || true
          fi
        # Inject API URL into frontend (set REACT_APP_API_URL in Amplify env to your API Gateway URL)
        - |
          REPO_ROOT="${CODEBUILD_SRC_DIR:-.}"
          [ -d "heat-transfer-hack" ] && REPO_ROOT="heat-transfer-hack"
          cd "$REPO_ROOT"
          API_URL="${REACT_APP_API_URL:-}"
          API_URL="${API_URL%/}"
          if [ -n "$API_URL" ]; then
            sed -i.bak "s|window.HEATGRID_API_BASE_URL = \"\"|window.HEATGRID_API_BASE_URL = \"$API_URL\"|" index.html
            echo "Injected HEATGRID_API_BASE_URL=$API_URL"
          fi
  artifacts:
    baseDirectory: heat-transfer-hack
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
