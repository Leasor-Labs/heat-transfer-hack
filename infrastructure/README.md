# Backend is now SAM (no CDK)

CDK has been removed. The backend is defined in the repo root **`template.yaml`** and deployed with **AWS SAM**.

See **BACKEND_DEPLOY.md** in the repo root.

You can delete this `infrastructure/` folder (and its `node_modules`) if you no longer need it.
